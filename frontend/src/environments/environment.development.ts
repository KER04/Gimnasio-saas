/** Configuración de DESARROLLO (reemplaza a environment.ts al servir/compilar
 * en modo development, ver `fileReplacements` en angular.json). */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000/api',
};
